# Test overrides: exclude OAuth2 client auto-configurations so that
# @WebMvcTest and other slice tests can start without a running Authentik server.
# The SecurityConfig.OpenConfig (permit-all) still provides the security filter chain.
spring.autoconfigure.exclude=\
  org.springframework.boot.security.oauth2.client.autoconfigure.OAuth2ClientAutoConfiguration,\
  org.springframework.boot.security.oauth2.client.autoconfigure.servlet.OAuth2ClientWebSecurityAutoConfiguration
